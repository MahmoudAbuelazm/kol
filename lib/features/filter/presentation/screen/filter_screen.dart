import 'package:flutter/material.dart';

import '../section/filter_body.dart';

class FilterScreen extends StatelessWidget {
  const FilterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:  FilterBody(
        
      ),
    );}
}